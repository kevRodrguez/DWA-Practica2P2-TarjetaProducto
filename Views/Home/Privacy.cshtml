@model TarjetaProductoViewModel

@{
    ViewData["Title"] = "Segunda pagina";
}

<section class="product-page">
    <div class="product-page__header">
        <h1 class="display-5 fw-semibold">@ViewData["Title"]</h1>
        <p class="lead mb-0">
            Aqui se muestra la misma partial view, pero con otro producto para evidenciar la reutilizacion.
        </p>
    </div>

    <partial name="_TarjetaProducto" model="Model" />
</section>
